#!/usr/bin/env ruby
# frozen_string_literal: true

require "gli"
require "subrepo/cli"
require "subrepo/version"

app = Subrepo::CLI.new
app.setup
begin
  result = app.run(ARGV)
rescue => e
  require 'pry'
  binding.pry
end

exit result
